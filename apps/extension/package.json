{
  "name": "@rediscover/extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.content.config.ts && node scripts/manifest.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rediscover/api-client": "0.0.0",
    "@rediscover/core": "0.0.0"
  },
  "devDependencies": {
    "@types/chrome": "0.0.326",
    "typescript": "5.8.3",
    "vite": "8.2.2"
  }
}
